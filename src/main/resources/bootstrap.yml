spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jeevamyil/pensionServiceCloudConfig.git
          skip-ssl-validation: true
          default-label: main

server:
    port: 8071